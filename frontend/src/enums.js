const GenAlgos = Object.freeze({
    HUNT: 'Hunt and kill',
    BACKTRACKING: 'Backtracking'
})

const SolveAlgos = Object.freeze({
    A_STAR: 'A*',
    DIJKSTRA: 'Dijkstra'
})

export {GenAlgos, SolveAlgos}